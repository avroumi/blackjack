services:
  
  app:
    container_name: black-jack
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ".:/app"
      - "node_modules:/node_modules"
      
    

volumes:
  node_modules:


  
